// #include "Enemy.h"
// #include "raylib.h"

// Enemy::Enemy() { 
// }

// void Enemy::enemyTrac(Vector2 *p, Vector2 *t, float *w) {
//         Vector2 &enemyPos = *p;
//         Vector2 &targetPos = *t;
//         float &width = *w;
//         if (targetPos.x > enemyPos.x) {enemyPos.x+=enemyspeed;Isleft = 1.0f;}
//         if (targetPos.x < enemyPos.x) {enemyPos.x-=enemyspeed;Isleft = -1.0f;}
//         if (targetPos.y > enemyPos.y) {enemyPos.y+=enemyspeed;}
//         if (targetPos.y < enemyPos.y) {enemyPos.y-=enemyspeed;}

//         // width = Isleft*width;

// }